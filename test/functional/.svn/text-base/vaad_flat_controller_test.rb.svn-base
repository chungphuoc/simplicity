require File.dirname(__FILE__) + '/../test_helper'
require 'vaad_flat_controller'

# Re-raise errors caught by the controller.
class VaadFlatController; def rescue_action(e) raise e end; end

class VaadFlatControllerTest < Test::Unit::TestCase
  def setup
    @controller = VaadFlatController.new
    @request    = ActionController::TestRequest.new
    @response   = ActionController::TestResponse.new
  end

  # Replace this with your real tests.
  def test_truth
    assert true
  end
end
